Target Environment: Claude Code (Terminal) or Copilot Chat Workspace.

You are a Staff Engineer building "Banker's Wrapped" for the Backblaze Generative Media Hackathon. 
Generate the complete directory structure and boilerplate configuration files based on our ARGUS blueprint.

Please create the following base configuration files with exact dependencies:
1. `pyproject.toml` using Poetry with: fastapi, uvicorn, semantic-kernel, pydantic-settings, genblaze, openai, boto3, structlog, pytest, pytest-mock, pytest-asyncio, ruff, mypy.
2. `backend/config.py` using Pydantic Settings to validate: GENBLAZE_API_KEY, NVIDIA_NIM_API_KEY, B2_ENDPOINT_URL, B2_KEY_ID, B2_APPLICATION_KEY, B2_BUCKET_NAME, and DATABASE_URL (defaulting to sqlite:///./session_store.db).
3. `backend/main.py` initializing a FastAPI app with structured logging via structlog, a standard request-duration logging middleware, and a CORS middleware configured for a local Next.js frontend.
4. `backend/api/v1/health.py` providing a clean health check endpoint returning status and timestamp.

Ensure strict type-hinting, adherence to Python 3.12 syntax, and zero placeholders or 'todo' comments.
